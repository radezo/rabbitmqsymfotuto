{% extends 'base.html.twig' %}

{% block title %}Personnes{% endblock %}

{% block body %}
    <h1>Personnes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personne.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ personne.adresse }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ personne.ville }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ personne.codePostal }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ personne.description }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ personne.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personnes_index') }}">back to list</a>

    <a href="{{ path('personnes_edit', {'id': personne.id}) }}">edit</a>

    {{ include('personnes/_delete_form.html.twig') }}
{% endblock %}
